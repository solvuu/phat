{
  "name": "phat",
  "version": "0.1.0-dev",
  "description": "Strongly type filepaths",
  "scripts": {
    "build": "dune build",
    "start": "dune build @all --profile dev -w",
    "utop": "dune utop",
    "clean": "rm -rf _build _build.prev"
  }
}
